   <h2>Users</h2>
   <table class="table">
     <thead>
       <tr>
        <th>image</th>
        <th>name</th>
        <th colspan="3"></th>
       </tr>
     </thead>
     <tbody>
       <% @users.each do |users| %>
       <tr>
        <td><%=%></td>
        <td><%= users.name %></td>
        <td><%= link_to "Show", others_path(users.id) %></td>
       </tr>
       <% end %>
     </tbody>
    </table>
